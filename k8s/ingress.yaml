---
# Ingress for external HTTPS access
# Requires: ingress-nginx controller and cert-manager
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fleet-ingress
  namespace: fleet
  labels:
    app.kubernetes.io/name: printer-fleet
    app.kubernetes.io/component: ingress
  annotations:
    # For cert-manager TLS
    cert-manager.io/cluster-issuer: letsencrypt-prod
    # Nginx ingress annotations
    nginx.ingress.kubernetes.io/proxy-body-size: "1024m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "86400"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "86400"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "86400"
    # WebSocket support - Force HTTP/1.1 for WebSocket upgrades
    nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
    nginx.ingress.kubernetes.io/upstream-hash-by: "$remote_addr"
    # Rate limiting
    nginx.ingress.kubernetes.io/limit-rps: "30"
    nginx.ingress.kubernetes.io/limit-connections: "100"
    # Allow HTTP for ACME challenges
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - fleet.modovolo.com
      secretName: fleet-tls
  rules:
    - host: fleet.modovolo.com
      http:
        paths:
          # Auth API endpoints
          - path: /api/auth
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 8080
          # Fleet API endpoints
          - path: /api/fleet
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 8080
          # Printers API endpoints (user's printers)
          - path: /api/printers
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 8080
          # Printer registration API endpoints
          - path: /api/printer-registration
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 8080
          # Health check API
          - path: /api/health
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 8080
          # WebSocket endpoints for fleet management
          - path: /ws/printer
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 9080
          - path: /ws/web
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 9081
          # WebSocket proxy for individual printer connections (Moonraker proxy)
          - path: /ws/client
            pathType: Prefix
            backend:
              service:
                name: fleet-manager
                port:
                  number: 9081
          # All other traffic goes through nginx-proxy which handles routing
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-proxy
                port:
                  number: 80
---
# ClusterIssuer for Let's Encrypt (if not already exists)
# Uncomment if you need to create cert-manager issuer
# apiVersion: cert-manager.io/v1
# kind: ClusterIssuer
# metadata:
#   name: letsencrypt-prod
# spec:
#   acme:
#     server: https://acme-v02.api.letsencrypt.org/directory
#     email: your-email@example.com
#     privateKeySecretRef:
#       name: letsencrypt-prod
#     solvers:
#       - http01:
#           ingress:
#             class: nginx
